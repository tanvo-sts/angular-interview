import { ReplaceTextDirective } from './replace-text.directive';

describe('ReplaceTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ReplaceTextDirective();
    expect(directive).toBeTruthy();
  });
});
